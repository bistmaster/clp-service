"use strict";

/**
 * @typedef {Object} CLPProperty
 * @property {array} keys return an array of keys
 * @property {number} length length of the object
 * @property {array} _fields returns the fields with properties
 * @property {array} _fieldLookup return the "n" lookup
 */
function CLPProperty() {}